name: languages-csharp-tools-test
version: 0.0.1
dependencies:
  # This test pack does not actually depend upon `codeql/csharp-queries`,
  # but we declare the dependency to ensure that the queries from the
  # query pack are downloaded and available locally.
  codeql/csharp-queries: "*"
  languages-csharp-tools-src: '*'
extractor: csharp
