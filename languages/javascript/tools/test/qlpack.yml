name: languages-javascript-tools-test
version: 0.0.1
dependencies:
  # This test pack does not actually depend upon `codeql/javascript-queries`,
  # but we declare the dependency to ensure that the queries from the
  # query pack are downloaded and available locally.
  codeql/javascript-queries: "*"
  languages-javascript-tools-src: '*'
extractor: javascript
